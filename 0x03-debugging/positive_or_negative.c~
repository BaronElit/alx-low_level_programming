#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
*main - entry point of programs
*
*Return: return 0
*/
int main(void)
{
int i;
if (i < 0)
{
printf("%d is negative\n", i);
}
else if (i > 0)
{
printf("%d is positive\n", i);
}
else
printf("%d is zero\n", i);
return (0);
}
