#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char echot[60];
strcpy(echot, "\"Programming is like building a multilingual puzzle\n");
puts(echot);
return (0);
}
